# Copy to .env (or .env.local) and fill in. NEVER commit a real .env.
# Both are safe to ship in the client bundle (the anon key is public by design;
# row-level security is the access boundary).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
